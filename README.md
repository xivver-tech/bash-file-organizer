# Bash File Organizer

A simple Bash script that organizes files into folders by extension.

## How to use

```bash
chmod +x organize.sh
./organize.sh          # organizes current directory
./organize.sh /path    # organizes a specific folder
```

Example: a file named `photo.jpg` will be moved into a `jpg/` folder.
